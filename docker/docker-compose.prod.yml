services:
  api:
    build:
      target: runner
    environment:
      NODE_ENV: production

  front:
    build:
      target: production
    ports:
      - "8080:80"
    environment:
      NODE_ENV: production
